<div class="container">
  <div class="row d-flex justify-content-center">
    <div class="col-6 rounded-container">
      <h2 class="mt-3">Create a new flat</h2>
      <%= simple_form_for(@flat) do |f| %>
        <%= f.input :title %>
        <%= f.input :address %>
        <%= f.input :description, as: :text %>
        <%= f.input :availability_start, as: :string, input_html: {class: "datepickerflat"} %>
        <%= f.input :availability_end, as: :string, input_html: {class: "datepickerflat"} %>
        <%= f.input :price_per_day %>
        <%= f.input :number_of_guests %>
        <%= f.input :photos, as: :file, input_html: { multiple: true } %>
        <%= f.submit "Create a flat", class: "btn blue-btn" %>
      <% end %>
      <%= link_to "Back to flats", flats_path, class: "btn lavander-btn mt-3" %>
    </div>
  </div>
</div>
